{
  "name": "http-fetch",
  "version": "0.1.0",
  "type": "module",
  "license": "Apache-2.0",
  "scripts": {
    "build": "tsc -p .",
    "conformance": "npx tsx ${CAK_HOME:-$HOME/agent-kernel}/bin/cak.ts conformance --subprocess \"node dist/main.js\" --contract http.fetch --args '{\"url\":\"https://example.com/\"}' --bad-args '{\"url\":\"http://127.0.0.1:1/\"}'"
  },
  "dependencies": {
    "@cak-dev/sdk": "^0.3.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/node": "^22.0.0"
  },
  "description": "CAK 插件：受控出网抓取（http.fetch@1）"
}
