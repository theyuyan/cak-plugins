{
  "name": "github",
  "version": "0.1.0",
  "type": "module",
  "license": "Apache-2.0",
  "scripts": {
    "build": "tsc -p .",
    "conformance": "npx tsx ${CAK_HOME:-$HOME/agent-kernel}/bin/cak.ts conformance --subprocess \"node dist/main.js\" --contract github.query --args '{\"path\":\"/rate_limit\"}'",
    "test": "node --test test.mjs"
  },
  "dependencies": {
    "@cak/sdk": "file:../../vendor/cak-sdk-0.3.0.tgz"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/node": "^22.0.0"
  },
  "description": "CAK 插件：GitHub（github.query 只读 REST + github.issue.create）"
}
