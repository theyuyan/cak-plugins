{
  "name": "memory-sqlite",
  "version": "0.1.0",
  "type": "module",
  "license": "Apache-2.0",
  "scripts": {
    "build": "tsc -p .",
    "conformance": "MEMORY_SQLITE_FILE=./test-mem.sqlite npx tsx ${CAK_HOME:-$HOME/agent-kernel}/bin/cak.ts conformance --subprocess \"node dist/main.js\" --contract memory.search --args '{\"query\":\"kernel\",\"limit\":5}' && MEMORY_SQLITE_FILE=./test-mem.sqlite npx tsx ${CAK_HOME:-$HOME/agent-kernel}/bin/cak.ts conformance --subprocess \"node dist/main.js\" --contract memory.write --args '{\"content\":\"conformance probe\"}'",
    "test": "node --test test.mjs"
  },
  "dependencies": {
    "@cak-dev/sdk": "^0.3.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/node": "^22.0.0"
  },
  "description": "CAK 插件：本地长期记忆（memory.search@1 + memory.write@1），SQLite FTS5"
}
