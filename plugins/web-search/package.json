{
  "name": "web-search",
  "version": "0.1.0",
  "type": "module",
  "license": "Apache-2.0",
  "scripts": {
    "build": "tsc -p .",
    "conformance": "WEB_SEARCH_CONFIG=/nonexistent.json npx tsx ${CAK_HOME:-$HOME/agent-kernel}/bin/cak.ts conformance --subprocess \"node dist/main.js\" --contract web.search --args '{\"query\":\"cak\"}'",
    "test": "node --test test.mjs"
  },
  "dependencies": {
    "@cak/sdk": "file:../../vendor/cak-sdk-0.3.0.tgz"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/node": "^22.0.0"
  },
  "description": "CAK 插件：网页搜索（web.search@1）— Brave / Tavily / SearXNG 适配，key 放文件"
}
